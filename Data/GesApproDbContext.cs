namespace GesAppro.Data
{
    // Lightweight placeholder DbContext to keep project layout consistent with other projects.
    // This stub avoids adding an EF Core dependency; if you want EF support, I can add
    // a proper DbContext and required NuGet packages (Microsoft.EntityFrameworkCore, provider, etc.).
    public class GesApproDbContext
    {
        // Intentionally empty placeholder. Use a real DbContext here when switching to EF Core.
    }
}
